services:
  - type: web
    name: ouvidoria-cetep-lnab
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd web_app && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 10000

